spring:
  application:
    name: user-service
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/user
    username: subinny
    password: mypassword

eureka:
  client:
    service-url:
      defaultZone: http://localhost:19090/eureka/


server:
  port: 19095

# 추가: auth-service의 URL 설정
auth:
  service:
    url: http://localhost:19092  # auth-service의 실제 URL을 입력